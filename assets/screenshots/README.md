# Screenshots

Drop three cropped Telegram screenshots here, then uncomment the `## Screenshots`
block near the top of the project [`README.md`](../../README.md).

Expected files (≈280 px wide, PNG):

| File             | What to capture                                              |
|------------------|-------------------------------------------------------------|
| `main-menu.png`  | The `/start` main menu (Monitoring / Management / Balance)  |
| `alert.png`      | A server up/down alert notification                         |
| `balance.png`    | The balance / cost view                                     |

Tip: crop to just the chat content (no device chrome) for a clean look. A short
demo GIF of toggling a server is even better — add it as `demo.gif` and reference it
the same way.
